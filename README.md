# Mi primera página web personal

Esta es mi primera página web hecha con HTML. Es muy sencilla pero incluye las etiquetas básicas para que empiece a practicar. La hice como parte de mi aprendizaje en desarrollo web.

## 📄 Descripción

Es una página personal con mi nombre, edad, estudios, experiencias y algunos datos sobre mí. Todo está escrito en HTML puro, sin estilos complicados, para que sea fácil de entender y modificar.

## 🚀 Cómo usarla

1. **Abrir en VS Code**:  
   - Abre Visual Studio Code.  
   - Crea un archivo nuevo (`Ctrl + N` o `Cmd + N`).  
   - Copia todo el código de `index.html` y pégalo.  
   - Guárdalo con el nombre `index.html` (¡importante que termine en `.html`!).

2. **Ver la página en el navegador**:  
   - Puedes hacer doble clic en el archivo `index.html` donde lo guardaste.  
   - O si tienes la extensión "Live Server" en VS Code, haz clic derecho sobre el archivo y elige "Open with Live Server".

3. **Personalizarla**:  
   - Cambia los textos por tus propios datos.  
   - Si quieres poner tu foto, descarga una imagen a la misma carpeta y cambia la línea:  
     `<img src="https://via.placeholder.com/150" alt="Mi foto">`  
     por algo como:  
     `<img src="mifoto.jpg" alt="Mi foto">`  
     (donde `mifoto.jpg` es el nombre de tu archivo de imagen).

## 📁 Archivos

- `Pumaistro.html` – El código de la página web.  

## 🧠 Etiquetas HTML que aprendí

- `<!DOCTYPE html>` – Le dice al navegador que usamos HTML5.
- `<html>` – La raíz de toda la página.
- `<head>` – Información para el navegador (no se ve).
- `<meta charset="UTF-8">` – Para usar acentos y ñ.
- `<title>` – Título de la pestaña.
- `<body>` – Todo lo visible.
- `<h1>`, `<h2>`, `<h3>` – Títulos y subtítulos.
- `<p>` – Párrafos.
- `<hr>` – Línea horizontal.
- `<ul>` y `<li>` – Listas con viñetas.
- `<ol>` y `<li>` – Listas numeradas.
- `<a>` – Enlaces (como el correo).
- `<img>` – Imágenes.
- `<!-- comentario -->` – Para dejar notas en el código.

## ✨ Siguientes pasos

- Aprender CSS para darle color y diseño.
- Agregar más secciones (como hobbies, proyectos).
- Subir la página a internet gratis con GitHub Pages.

## 💬 Créditos

Creado por César Alexis Aranda Meléndez – 2026  
Si tienes dudas o sugerencias, escríbeme a cesar.aranda.2105@gmail.com
